## [2026-04-25 19:35] | Task: Price GPT-5.5 costs

### 🤖 Execution Context

- **Agent ID**: `Codex`
- **Base Model**: `GPT-5`
- **Runtime**: `Codex CLI`

### 📥 User Query

> Today 一直显示 $0.00，确认是不是 cost 没算出来；Today 和 ~30 Days 应该是多个账号合起来的总计。

### 🛠 Changes Overview

**Scope:** `Sources/AgentBarCore`

**Key Actions:**

- **[Pricing]**: 为 `gpt-5.5` 增加 Codex token 成本价格，避免今天的 `gpt-5.5` session 只统计 token、不统计 cost。
- **[Cache invalidation]**: 只对已缓存的 `gpt-5.5` 零成本 cost 文件做重算，避免清空启动所需的 latest snapshot 和账号 quota 缓存；同时兼容短暂写出的 v3 cache 文件。
- **[Startup recovery]**: 当完整 snapshot 缺失但 cost 文件缓存还在时，启动先从 cost 文件缓存恢复 Today/~30 Days；账号 quota 为空时先单独拉取账号 usage，不再被 30 天 cost 扫描阻塞。
- **[Tests]**: 增加 `gpt-5.5` token_count 成本测试和旧零成本缓存重算测试，保留 Today 和 ~30 Days 的本机全局汇总语义。

### 🧠 Design Intent (Why)

Today 和 ~30 Days 来自本机 session JSONL 的全局 token/cost 扫描，不应该跟当前账号切换绑定。当前 `$0.00` 的直接原因是新模型 `gpt-5.5` 没有价格表条目，scanner 能汇总 token 但算不出成本。价格表更新只应该让相关 cost 文件重算，不能通过全局 cache version 升级丢掉账号 quota 启动缓存；如果启动缓存不完整，也应该尽快从可用的 cost/account 数据局部恢复，而不是一直显示默认空态。

### 📁 Files Modified

- `Sources/AgentBarCore/CodexPricing.swift`
- `Sources/AgentBarCore/AgentBarCacheStore.swift`
- `Sources/AgentBarCore/CodexCostScanner.swift`
- `Sources/AgentBarCore/CodexSnapshotService.swift`
- `Sources/AgentBar/App.swift`
- `Tests/AgentBarCoreTests/CodexCostScannerTests.swift`
- `docs/ARCHITECTURE.md`
- `docs/histories/2026-04/20260425-1935-price-gpt55-costs.md`
